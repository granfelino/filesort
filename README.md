# A file sorting app
A simple file sorting app scanning, classifying and copying into appropriate 
type directories files found in a location pointed by the user.

### Usage:
`python -m filesort src dst` 

where `src` is the source directory and `dst` is the destination directory.
