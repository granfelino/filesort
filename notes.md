# Functions I need:
* os.listdir()
* os.mkdir()
* os.makedirs() -- same as `mkdir -p`
* os.scandir() -- better than os.listdir(), because it returns an iterator that
                  contains entries with relevant file/directory info
* os.scandir.close() -- closes the iterator and frees resources 
* os.sync() -- force writing everything to disk
* os.path.join() -- create a path from directory names
* os.path.exists() -- bool
* os.path.isfile() -- bool
* os.path.isdir() -- bool
* os.walk() -- generates an iterable object with all dirs and files in them


* pathlib.Path.exists() -- bool
* pathlib.Path.is_file() -- 
* pathlib.Path.is_dir() -- 
* pathlib.Path.iterdir() -- list directory contents
* pathlib.Path.walk()
* pathlib.Path.mkdir()
* pathlib.Path.copy_into()
* pathlib.PurePath.suffix()
* pathlib.PurePath.suffixes()


* shutil.copy()
